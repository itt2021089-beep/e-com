server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This line is critical for React Router (Single Page Applications)
        try_files $uri $uri/ /index.html;
    }
}